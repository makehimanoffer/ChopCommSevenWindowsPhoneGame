using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ChopComm7
{
   public  class EnemyManager
    {
       /*
       List<Enemy> enemies = new List<Enemy>();
       public int noOfEnemies;
       public  int noOfOtherEnemies;
       public int noOfOtherOtherEnemies;
       public bool alive=false;
       int i;
       public EnemyManager(int noOfEnemyChoppers, int noOfOtherEnemies, int noOfOtherOtherEnemies)
       {
           this.noOfEnemies = noOfEnemyChoppers;
           this.noOfOtherEnemies=noOfOtherEnemies;
           this.noOfOtherOtherEnemies=noOfOtherOtherEnemies;

           //this constructor will be used in the wave manager class eventually for determining what will spawn in each wave
       }

       public void Initialize(){
          
       }

       public bool checkLives(WaveManager wave){

           // this method should check if all the things in it are dead and if so will return a boolean to increment wave, add new enemymanager and then remove the one with all the dead things
           //it doesn't do this yet... bit of a Catch 22/ Race Condition going on in terms of whether WaveManager should come first or whatnot.
           foreach (Enemy e in enemies)
           {
               if (e.alive == false)
               {
                   i=i+1;
               }
           }

           if(i==noOfEnemies){
               return false;
           }
           else
           {
               return true;
           }

         
               ///not sure if this should be here or in WaveManager..
       }

           
      
       */
    }
}
